# Bee 🐝

Create parametric geometry nodes in Blender using Python.

## Usage

Replace and add to the top of your script:

```python
import sys
sys.path.append("/../path/../src")

from bee import nodes as nds
```

## Install as local package (optional)

```bash
pip install -e .
```